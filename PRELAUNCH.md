# Pre-Launch Placeholder Swap List

Everything in this file is a **placeholder that must not ship**. Check each
item off before the App Store submission build.

## Art

- [ ] **Hero sprites** — currently colored circles with a white "nose"
  (`HeroNode.swift`). Need per-class sprites with walk/attack animations
  (5 classes × 4 directions).
- [ ] **Enemy sprites** — colored circles (`EnemyNode` + factories in
  `Game/Enemies/`). Need: Slime, Mushroom, Jr. Boogie idle/move/death.
- [ ] **King Slime boss sprite** — large blue circle. Needs real art +
  phase-2 "enraged" variant.
- [ ] **Projectiles** — cyan/gold circles (`ProjectileNode.swift`).
- [ ] **Room tiles & obstacles** — green wireframe walls + grey rounded
  rects (`GameScene.setupRoom`). Need tileset (grass/ruins theme, chapter 1).
- [ ] **Door** — grey/gold rounded rect (`DoorNode.swift`).
- [ ] **Gold orbs** — yellow circles (`GoldOrbNode.swift`).
- [ ] **Death particles** — green circle shards (`GameScene.spawnDeathBurst`);
  replace with themed splat matching enemy art.
- [ ] **Lobby island art** — SwiftUI-shape trees/platform
  (`MainMenuView.FloatingIsland`). Design slot exists for real art
  ("image slot" variant in the Claude Design file).
- [ ] **Character tab hero art** — "CHARACTER ART HERE" placeholder frame
  (`CharacterView.heroPlaceholder`).
- [ ] **Class emblems** — colored circles in class cards (`CharacterView`,
  class picker).
- [ ] **Gear item icons** — text abbreviations (AXE/RING/…) in
  `EquipSlotView`. Need icon per slot × rarity treatment.
- [ ] **App icon** — still the Xcode default (empty `AppIcon.appiconset`).
- [ ] **Launch screen** — default.

## Audio

- [ ] **All 8 SFX are synthesized placeholders** (`Resources/Sounds/*.wav`,
  generated by script — square/saw waves). Target: soft, bouncy, casual
  profile (MapleStory-adjacent). Replace: shoot, hit, crit, enemy_death,
  pickup, door_open, hero_hit, boss_slam. Budget ~$100–200 licensed packs
  or commission.
- [ ] **Music — none exists.** Need: lobby loop, combat loop, boss theme
  (minimum viable: 2 tracks).
- [ ] **Missing SFX to add with the real set**: skill-choice card select
  (per rarity), starforce success/fail/destroy, cube roll/rank-up, chest
  open, level up, button tap, run victory/defeat stingers.

## Monetization note

In-run skill selection is now free pick-1-of-3 (no tokens). Contextual
IAP offers attach to the equipment gamble instead: safety charms
(starforce), cube packs, gem top-ups at low-resource moments.

## Copy / Identity

- [ ] **Game name** — "MapleRogue" references Nexon's IP. Legal risk in a
  commercial App Store release; pick an original name before submission.
- [ ] **MapleStory-derived names in code/UI** — "Henesys Ruins", "Mesos",
  enemy names (Jr. Boogie, King Slime), class names (Dark Knight, Night
  Lord, etc.), item names (Emberfang Axe is fine; check the rest). Rename
  to original equivalents — keep the *vibe*, drop the trademarks.
- [ ] **Player name** — hardcoded "Adventurer"; add name entry or generate.
- [ ] **Season banner** — static "SEASON 1 / Lv 1" placeholder in lobby
  (`MainMenuView.seasonRow`); wire to real season system or hide for v1.

## Systems stubs (visible to players)

- [ ] **Gem packs** — display-only, tap shows "purchases arrive in a later
  build" (`ShopView`). Wire StoreKit 2 or hide the section.
- [ ] **Cash / Pet tabs** — empty states (`CharacterView`). Hide for v1 if
  the features don't ship.
- [ ] **Quests / Events rail buttons** — "SOON" badges (`MainMenuView`).
  Fine to keep if intentional; remove if not.
- [ ] **Hamburger menu button** — does nothing (`MainMenuView.seasonRow`).
  Needs settings menu (sound toggle exists in code: `SoundSystem.isMuted`).

## Ship checklist (not placeholders, but required)

- [ ] Privacy manifest + App Privacy questionnaire
- [ ] Age rating questionnaire: flag In-Game Purchases + Random Items
  (loot boxes) once IAP ships
- [ ] Store listing discloses randomized paid items
- [ ] Screenshots + preview video from real (non-placeholder) build
